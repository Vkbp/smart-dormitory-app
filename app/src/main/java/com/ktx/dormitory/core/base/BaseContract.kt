package com.ktx.dormitory.core.base

/**
 * Standard MVI-lite Contract interface.
 * Defines the structure for UI State, Events, and Effects.
 */
interface BaseContract {
    interface State
    interface Event
    interface Effect
}
